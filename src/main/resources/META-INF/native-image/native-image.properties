# GraalVM Native Image configuration for BouncyCastle
# Initialize all BC classes at build time to avoid runtime classpath issues
# on GraalVM Native Image + Java 25.
Args = --initialize-at-build-time=org.bouncycastle.**
